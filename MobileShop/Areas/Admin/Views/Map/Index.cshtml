@model IEnumerable<Model.EF.Map>

@{
    ViewBag.Title = "Danh sách bản đồ";
}

<div class="row">
    <div class="col-lg-12">
        <h2 class="page-header">Danh sách bản đồ</h2>
    </div>
    <!-- /.col-lg-12 -->
</div>
<!-- /.row -->
<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <!-- /.panel-heading -->
            <div class="panel-body">
                @using (Html.BeginForm("Delete", "Map"))
                {
                    <p>
                        <button type="submit" onclick="return confirm('Bạn có chắc là muốn xóa không')" class="btn btn-danger"><i class="fa fa-trash"></i> Delete</button>
                        <a href="@Url.Action("Create")" class="btn btn-default"><i class="fa fa-plus"></i> Create New</a>
                    </p>
                    if (TempData["AlertMessage"] != null)
                    {
                        <div id="alertBox" class="alert alert-@TempData["AlertType"] text-center hide"> @Html.Raw(TempData["AlertMessage"])</div>
                    }
                    <table width="100%" class="table table-striped table-bordered table-hover">

                        <thead>
                            <tr>
                                <th>@Html.CheckBox("chkDeleteAll", new { id = "chkDeleteAll" })</th>
                                <th width="200">@Html.DisplayNameFor(model => model.Name)</th>
                                <th>@Html.DisplayNameFor(model => model.APIKey)</th>
                                <th>@Html.DisplayNameFor(model => model.Latitude)</th>
                                <th>@Html.DisplayNameFor(model => model.Longitude)</th>
                                <th>@Html.DisplayNameFor(model => model.Status)</th>
                                <th>Chức năng</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var map in Model)
                            {
                                <tr>
                                    <td>@Html.CheckBox("mapId", htmlAttributes: new { @class = "chkFieldId", value = map.Id })</td>
                                    <td>@map.Name</td>
                                    <td>@map.APIKey</td>
                                    <td>@map.Latitude</td>
                                    <td>@map.Longitude</td>
                                    <td>@(map.Status == true ? "Hiển thị" : "Ẩn" )</td>
                                    <td><a class="" href="@Url.Action("Edit", new { id = map.Id })"><i class="fa fa-edit"></i> Edit</a></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                }
            </div>
            <!-- /.panel-body -->
        </div>
        <!-- /.panel -->
    </div>
    <!-- /.col-lg-12 -->
</div>
<!-- /.row -->
